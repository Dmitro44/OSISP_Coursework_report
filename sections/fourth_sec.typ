#import "../lib/stp2024.typ"

= Проектирование функциональных возможностей программы

Реализация #emph("DNS")-сервера требует разработки чёткого алгоритма обработки входящих запросов. В предыдущих разделах были рассмотрены архитектурные решения, структура протокола и организация хранения данных. Настоящий раздел посвящён проектированию логики обработки запроса: от момента получения пакета до формирования и отправки ответа клиенту.

== Алгоритм разбора входящего DNS-запроса

Первым этапом обработки запроса является извлечение структурированной информации из бинарного потока данных, полученного по протоколу #emph("UDP"). Пакет представляет собой последовательность байт, в которой каждый элемент имеет строго определённое положение и назначение в соответствии со стандартом @rfc1035.

Процесс разбора начинается с извлечения заголовка #emph("DNS")-пакета. Первые 12 байт полученной датаграммы интерпретируются как структура фиксированного формата. Поле #emph("ID") извлекается из байтов 0--1 с учётом сетевого порядка следования байт. Данный идентификатор должен быть скопирован в ответ без изменений, что позволяет клиенту сопоставить ответ с исходным запросом. Байты 2 и 3 содержат набор флагов: бит #emph("QR") определяет тип сообщения, поле #emph("Opcode") -- тип операции, флаг #emph("RD") -- требование рекурсии. Проверка флага #emph("QR") позволяет отфильтровать некорректные пакеты: если значение не равно нулю, пакет является ответом, а не запросом, и должен быть отброшен. Поле #emph("RCODE") в запросе должно быть нулевым, иное значение сигнализирует о нарушении протокола.

Счётчики секций (#emph("QDCOUNT"), #emph("ANCOUNT"), #emph("NSCOUNT"), #emph("ARCOUNT")) извлекаются из байтов 4--11. Корректный стандартный запрос содержит ровно одну запись в секции вопросов, поэтому значение #emph("QDCOUNT") должно равняться единице. Счётчики секций ответа в запросе должны быть нулевыми. Если эти условия не выполняются, пакет считается некорректным и отбрасывается с фиксацией ошибки в журнале.

После извлечения и валидации заголовка следует разбор секции запроса. Эта секция начинается с 13-го байта пакета и содержит кодированное доменное имя (#emph("QNAME")), за которым следуют двухбайтовые поля #emph("QTYPE") и #emph("QCLASS"). Декодирование доменного имени осуществляется путём последовательного чтения меток. Каждая метка начинается с байта длины, определяющего количество следующих за ним символов. Значение длины от 1 до 63 указывает на обычную метку, значение 0 означает конец имени. Если старшие два бита байта длины установлены в единицу, следующие 14 бит интерпретируются как смещение для механизма сжатия. Извлечённые метки объединяются в полное доменное имя путём конкатенации через символ точки.

Поле #emph("QTYPE"), расположенное сразу после кодированного имени, определяет тип запрашиваемой ресурсной записи. Значения 1, 28 и 5 соответствуют типам #emph("A"), #emph("AAAA") и #emph("CNAME"). Значение 255 означает запрос всех доступных записей. Поле #emph("QCLASS") должно иметь значение 1, соответствующее классу #emph("IN"). Иные значения, как правило, не поддерживаются и приводят к формированию ответа с кодом ошибки.

Результатом процесса разбора является структура данных, содержащая идентификатор запроса, извлечённое доменное имя, тип и класс запроса. Эта информация передаётся на следующий этап обработки -- поиск соответствующих записей.

== Двухуровневая система поиска ресурсных записей

Обработка запроса после этапа разбора переходит к поиску соответствующих ресурсных записей. Поиск осуществляется в два этапа: сначала в кэше оперативной памяти, затем -- при отсутствии данных в кэше -- в загруженном файле зоны. Такая двухуровневая архитектура обеспечивает баланс между скоростью доступа и полнотой данных.

=== Поиск в кэше и проверка актуальности записей

Первым этапом поиска является обращение к кэшу. Ключом поиска служит пара значений: доменное имя и тип записи. Если ранее был выполнен запрос с теми же параметрами, результат мог быть сохранён в кэше, и повторное обращение к файлу зоны не требуется.

Кэш представляет собой ассоциативный контейнер, в котором каждая запись сопровождается меткой времени добавления и значением #emph("TTL"). При обнаружении записи выполняется проверка её актуальности: вычисляется разность между текущим временем и временем сохранения записи. Если эта разность превышает значение #emph("TTL"), запись считается устаревшей и удаляется из кэша. Запрос обрабатывается так, как если бы записи в кэше не было.

Если запись найдена и не истекла, она извлекается из кэша и передаётся на этап формирования ответа. При этом в ответе должно быть указано оставшееся время жизни записи: из исходного значения #emph("TTL") вычитается время, прошедшее с момента сохранения. Это гарантирует, что клиент не будет хранить запись дольше, чем предусмотрено авторитативным сервером.

Успешное обнаружение записи в кэше завершает процесс поиска, и выполнение переходит к этапу формирования ответа. Если запись не найдена или оказалась устаревшей, процесс переходит ко второму этапу -- поиску в файле зоны.

=== Поиск в файле зоны и обработка результата

Файл зоны содержит полный набор авторитативных записей для обслуживаемого домена. Данные файла зоны загружаются в память при запуске сервера и организованы в виде структуры данных, обеспечивающей эффективный поиск по доменному имени.

Поиск в зоне осуществляется по доменному имени, извлечённому из запроса. Если домен найден, извлекаются все ресурсные записи, ассоциированные с этим именем. Далее выполняется фильтрация по типу: если клиент запрашивал тип #emph("A"), отбираются только записи этого типа. Если запрашивался тип #emph("ANY"), возвращаются все записи домена независимо от типа.

Особый случай представляет обработка записей типа #emph("CNAME"). Если для запрашиваемого имени существует запись #emph("CNAME"), сервер должен вернуть её клиенту и, по возможности, дополнить ответ записями канонического имени. Для этого выполняется второй поиск -- уже по каноническому имени, на которое указывает #emph("CNAME"). Если каноническое имя находится в той же зоне, соответствующие #emph("A")- или #emph("AAAA")-записи добавляются в секцию ответа. Такое поведение исключает необходимость повторного запроса со стороны клиента.

Найденные записи формируют список результатов, который передаётся на этап построения ответа. Дополнительно эти записи сохраняются в кэше с указанием текущего времени и значения #emph("TTL") из зоны, что позволяет обработать последующие идентичные запросы без обращения к файлу зоны.

Если ни одной записи для запрашиваемого имени в зоне не обнаружено, процесс переходит к обработке несуществующего домена.

== Формирование DNS-ответа

Этап формирования ответа преобразует структурированные данные, полученные на предыдущих этапах, в бинарное представление #emph("DNS")-пакета, готовое к отправке клиенту. Структура ответа соответствует структуре запроса: заголовок фиксированного размера, за которым следуют секции данных.

Построение заголовка ответа начинается с копирования идентификатора из запроса. Поле #emph("ID") переносится без изменений, что позволяет клиенту сопоставить ответ с запросом. Флаг #emph("QR") устанавливается в значение 1, сигнализируя, что пакет является ответом. Поле #emph("Opcode") копируется из запроса. Флаг #emph("AA") (Authoritative Answer) устанавливается в единицу, если запись была получена из файла зоны, и в ноль, если запись извлечена из кэша. Флаг #emph("RD") копируется из запроса. Флаг #emph("RA") (Recursion Available) устанавливается в ноль, поскольку в простой реализации рекурсивное разрешение запросов, как правило, не выполняется. Поле #emph("RCODE") устанавливается в значение 0 при успешном выполнении запроса.

Счётчики секций заполняются в соответствии с количеством записей в ответе. Поле #emph("QDCOUNT") устанавливается в значение 1, поскольку запрос дублируется в ответе. Поле #emph("ANCOUNT") содержит количество найденных ресурсных записей. Поля #emph("NSCOUNT") и #emph("ARCOUNT"), как правило, устанавливаются в ноль в упрощённых реализациях.

После заголовка в пакет записывается секция запроса. Она содержит те же данные, что были получены от клиента: кодированное доменное имя, тип и класс запроса. Эта секция позволяет клиенту однозначно идентифицировать, на какой вопрос получен ответ, особенно в случае параллельной обработки множества запросов.

Секция ответа формируется путём последовательной сериализации каждой найденной ресурсной записи. Запись начинается с кодирования доменного имени. Для уменьшения размера пакета применяется механизм сжатия: если доменное имя совпадает с именем в секции запроса, вместо повторного кодирования используется двухбайтовый указатель на смещение 12 (начало секции запроса). Старшие два бита указателя устанавливаются в единицу, остальные 14 бит содержат смещение.

За именем следуют двухбайтовые поля #emph("TYPE") и #emph("CLASS"), определяющие тип и класс записи. Поле #emph("TTL") записывается как 32-битное беззнаковое целое в сетевом порядке байт. Для записей из кэша значение #emph("TTL") уменьшается на время, прошедшее с момента сохранения. Поле #emph("RDLENGTH") содержит длину данных, следующих за ним. Для записи типа #emph("A") это значение равно 4, для типа #emph("AAAA") -- 16. Поле #emph("RDATA") содержит сами данные: адрес #emph("IPv4"), адрес #emph("IPv6") или кодированное доменное имя для записи #emph("CNAME").

Сформированный пакет отправляется клиенту по протоколу #emph("UDP") на адрес и порт, указанные в качестве источника входящего запроса. Использование #emph("UDP") позволяет серверу не хранить состояние соединения: каждый запрос обрабатывается независимо, а ответ отправляется непосредственно на адрес отправителя.

== Обработка несуществующих доменов и негативное кэширование

Запрос к несуществующему домену является частым случаем в реальной эксплуатации. Ошибки в конфигурации клиентов, опечатки в адресах, атаки на основе случайных поддоменов -- все эти ситуации приводят к запросам имён, отсутствующих в зоне. Корректная обработка таких запросов требует формирования ответа специального вида и, при необходимости, сохранения отрицательного результата в кэше.

=== Формирование ответа NXDOMAIN

Когда поиск в файле зоны не обнаружил ни одной записи для запрашиваемого доменного имени, сервер формирует ответ с кодом результата #emph("NXDOMAIN"). Согласно стандарту @rfc1035, этот код имеет числовое значение 3 и означает, что запрашиваемое доменное имя не существует.

Формирование ответа #emph("NXDOMAIN") отличается от формирования успешного ответа установкой поля #emph("RCODE") в заголовке. Все остальные компоненты заголовка заполняются стандартным образом: идентификатор копируется из запроса, флаг #emph("QR") устанавливается в единицу, флаг #emph("AA") устанавливается в единицу, так как сервер авторитативен для зоны и может достоверно утверждать, что домен не существует. Счётчик #emph("QDCOUNT") устанавливается в значение 1, счётчик #emph("ANCOUNT") -- в ноль, поскольку секция ответа остаётся пустой.

Секция запроса дублируется в ответе так же, как и в случае успешного ответа. Секция ответа остаётся пустой, что сигнализирует клиенту об отсутствии данных. Клиент, получив пакет с кодом #emph("NXDOMAIN"), интерпретирует это как окончательное отсутствие запрашиваемого домена в зоне.

Следует отличать код #emph("NXDOMAIN") от другого типа отрицательного ответа -- #emph("NODATA"). Если доменное имя существует в зоне, но для него нет записей запрашиваемого типа, код результата остаётся нулевым, однако секция ответа остаётся пустой. Например, если клиент запрашивает #emph("AAAA")-запись для домена, у которого есть только #emph("A")-записи, сервер возвращает успешный ответ с пустой секцией ответа. Код #emph("NXDOMAIN") используется исключительно в случае отсутствия самого доменного имени.

=== Негативное кэширование

Стандарт @rfc2308 вводит понятие негативного кэширования -- сохранения информации о несуществующих доменах или отсутствующих типах записей. Без такого механизма каждый повторный запрос к несуществующему домену проходил бы через весь цикл обработки, включая поиск в файле зоны, что создаёт излишнюю нагрузку на сервер.

Негативное кэширование осуществляется путём сохранения в кэше специальной записи, указывающей на отсутствие данных для конкретной пары «доменное имя, тип запроса». Время жизни негативной записи определяется значением #emph("TTL") из записи #emph("SOA") авторитативной зоны или устанавливается в фиксированное значение по умолчанию, например, 300 секунд.

При получении повторного запроса к тому же несуществующему домену сервер обнаруживает негативную запись в кэше и формирует ответ #emph("NXDOMAIN") без обращения к файлу зоны. Это значительно снижает время обработки запроса и уменьшает нагрузку на систему хранения данных.

Негативное кэширование требует осторожности при реализации. Если домен был создан в зоне после сохранения негативной записи, клиенты будут получать устаревший отрицательный ответ до истечения #emph("TTL"). Поэтому время жизни негативных записей обычно устанавливается меньшим, чем для позитивных. Типичное значение составляет от нескольких минут до получаса, что обеспечивает баланс между производительностью и актуальностью данных.

При изменении содержимого зоны, например, при добавлении новых доменов, рекомендуется выполнять очистку кэша или перезапуск сервера. Это гарантирует, что новые записи станут доступны клиентам немедленно, а не после истечения времени жизни негативных записей.

== Многопоточная архитектура сервера

Обеспечение высокой пропускной способности #emph("DNS")-сервера требует параллельной обработки множественных входящих запросов. Последовательная обработка, при которой сервер принимает новый запрос только после завершения обработки предыдущего, приводит к увеличению времени ожидания клиентов и снижению общей производительности системы. Многопоточная архитектура позволяет эффективно использовать вычислительные ресурсы многоядерных процессоров и обеспечивает одновременное обслуживание нескольких запросов.

=== Модель "приёмник -- пул потоков"

Реализация многопоточности основывается на архитектурном паттерне "приёмник -- пул потоков" @thread_pool. В данной модели существует главный поток, отвечающий за приём входящих #emph("UDP")-датаграмм, и фиксированное количество рабочих потоков, осуществляющих обработку запросов. Разделение ответственности между приёмом и обработкой обеспечивает высокую отзывчивость сервера: главный поток не блокируется на время выполнения операций разбора, поиска и формирования ответа, а немедленно возвращается к ожиданию следующего запроса.

Главный поток выполняет бесконечный цикл приёма данных из сокета. Получив датаграмму, он помещает пакет вместе с адресом отправителя в потокобезопасную очередь задач и продолжает ожидание следующего запроса. Рабочие потоки, в свою очередь, извлекают задачи из очереди, выполняют полный цикл обработки запроса и отправляют ответ клиенту. Если очередь пуста, рабочие потоки блокируются до появления новой задачи, что исключает излишнее потребление процессорного времени в режиме ожидания.

Количество рабочих потоков определяется на этапе инициализации сервера и соответствует числу доступных аппаратных потоков. Использование функции #emph("std::thread::hardware_concurrency()") позволяет автоматически адаптировать размер пула к характеристикам целевой платформы. Создание избыточного количества потоков приводит к непроизводительным затратам на переключение контекста, тогда как недостаточное количество не позволяет полностью использовать вычислительные ресурсы.

=== Использование std::jthread и std::stop_token для корректного \ завершения

Управление жизненным циклом рабочих потоков осуществляется с помощью класса #emph("std::jthread"), введённого в стандарте #emph("C++20"). В отличие от #emph("std::thread"), класс #emph("std::jthread") автоматически запрашивает остановку потока при уничтожении объекта и ожидает его завершения, что исключает необходимость явного вызова методов #emph("join()") или #emph("detach()"). Это упрощает обработку исключений и гарантирует корректное завершение всех потоков при выходе из области видимости.

Механизм остановки потоков реализуется с использованием токена #emph("std::stop_token"). Каждый рабочий поток получает токен в качестве параметра функции и периодически проверяет его состояние с помощью метода #emph("stop_requested()"). Когда главный поток инициирует завершение работы сервера, он вызывает метод #emph("request_stop()") у каждого объекта #emph("std::jthread"), что устанавливает флаг остановки в соответствующем токене. Рабочие потоки обнаруживают изменение состояния токена и корректно завершают текущую итерацию цикла обработки, освобождая ресурсы и выходя из функции.

Интеграция #emph("std::stop_token") с потокобезопасной очередью задач требует поддержки прерываемого ожидания. Операция извлечения элемента из очереди должна принимать токен остановки и возвращать управление при установке флага, даже если очередь остаётся пустой. Это гарантирует, что рабочие потоки не останутся заблокированными на операции ожидания при завершении работы сервера.

=== Потокобезопасная очередь задач

Взаимодействие между главным потоком и рабочими потоками осуществляется через потокобезопасную очередь задач. Очередь представляет собой обёртку над стандартным контейнером #emph("std::queue"), дополненную механизмами синхронизации, обеспечивающими корректность одновременного доступа из нескольких потоков.

Защита внутреннего состояния очереди обеспечивается мьютексом #emph("std::mutex"). Каждая операция вставки или извлечения элемента выполняется под блокировкой мьютекса, что исключает возможность гонки данных. Использование блокировки с автоматическим освобождением (#emph("std::lock_guard") или #emph("std::unique_lock")) гарантирует освобождение мьютекса даже в случае возникновения исключения.

Механизм ожидания появления элементов в очереди реализуется с помощью условной переменной #emph("std::condition_variable"). Когда рабочий поток обнаруживает, что очередь пуста, он вызывает метод #emph("wait()") условной переменной, передавая ей захваченный мьютекс. Это атомарно освобождает мьютекс и переводит поток в режим ожидания. Главный поток, добавив элемент в очередь, вызывает метод #emph("notify_one()") условной переменной, что пробуждает один из ожидающих рабочих потоков. Пробуждённый поток повторно захватывает мьютекс, проверяет наличие элемента и извлекает его из очереди.

Для корректной интеграции с механизмом остановки потоков метод ожидания должен поддерживать предикат и токен остановки. Вместо простого вызова #emph("wait()") используется #emph("wait()") с предикатом, проверяющим как наличие элемента в очереди, так и состояние токена остановки. Это позволяет прервать ожидание при запросе остановки, даже если очередь остаётся пустой.

=== Синхронизация доступа к кэшу

Кэш #emph("DNS")-записей представляет собой разделяемый ресурс, к которому обращаются все рабочие потоки. Без должной синхронизации одновременный доступ к структуре данных кэша приводит к неопределённому поведению программы, включая повреждение данных и аварийное завершение.

Для синхронизации доступа к кэшу используется механизм #emph("reader-writer lock"), реализованный с помощью класса #emph("std::shared_mutex"). Данный тип мьютекса позволяет разграничить операции чтения и записи: множество потоков могут одновременно удерживать блокировку на чтение, но только один поток может удерживать блокировку на запись, при этом все операции чтения блокируются.

Операции поиска записи в кэше выполняются под разделяемой блокировкой #emph("std::shared_lock"). Поскольку поиск не изменяет структуру кэша, несколько рабочих потоков могут одновременно выполнять операции чтения, что обеспечивает высокую степень параллелизма и минимизирует конкуренцию за доступ к кэшу.

Операции вставки новых записей и удаления устаревших записей требуют эксклюзивной блокировки #emph("std::unique_lock"). Такая блокировка гарантирует, что в момент модификации кэша ни один другой поток не осуществляет чтение или запись, что исключает возможность повреждения внутренней структуры контейнера.

Проверка актуальности записи, включающая сравнение времени жизни с текущим временем, выполняется под разделяемой блокировкой. Если запись оказывается устаревшей, блокировка повышается до эксклюзивной для выполнения удаления. Повышение блокировки требует её освобождения и повторного захвата в эксклюзивном режиме, что создаёт окно, в течение которого другой поток может модифицировать кэш. Поэтому после повторного захвата необходимо повторно проверить наличие записи и её актуальность.

=== Разделение операций приёма и обработки

Разделение ответственности между приёмом данных и их обработкой является ключевым архитектурным решением, обеспечивающим высокую пропускную способность сервера. Главный поток выполняет только минимально необходимые операции: вызов системной функции #emph("recvfrom()"), копирование полученных данных в буфер и помещение задачи в очередь. Все операции, требующие вычислительных затрат -- разбор пакета, поиск в кэше, поиск в зоне, формирование ответа -- делегируются рабочим потокам.

Такое разделение позволяет главному потоку возвращаться к ожиданию следующего запроса максимально быстро, что исключает потерю пакетов при высокой интенсивности входящего трафика. Операционная система буферизует входящие #emph("UDP")-датаграммы в очереди сокета, однако размер этой очереди ограничен. Если главный поток задерживается на обработке запроса, очередь переполняется, и новые пакеты отбрасываются на уровне ядра операционной системы. Немедленное извлечение пакета из очереди сокета и передача его в очередь задач уменьшает вероятность переполнения и повышает надёжность обслуживания.

Рабочие потоки, в свою очередь, не взаимодействуют с сокетом на этапе приёма данных. Они получают уже извлечённые датаграммы из очереди задач, что устраняет необходимость синхронизации доступа к сокету. Отправка ответа выполняется каждым рабочим потоком независимо с использованием функции #emph("sendto()"), которая, как правило, является потокобезопасной для операций записи в различные адреса назначения.

== Логирование и диагностика

Диагностика работы сервера и анализ поступающих запросов требуют ведения журнала событий. Логирование позволяет отслеживать корректность обработки запросов, выявлять ошибки в конфигурации зоны, анализировать эффективность кэша и диагностировать сетевые проблемы. Структурированное ведение журнала является неотъемлемой частью промышленной реализации серверного программного обеспечения.

=== Вывод информации о полученном запросе

Каждый принятый запрос регистрируется в журнале с указанием параметров запроса и адреса клиента. Запись включает метку времени, #emph("IP")-адрес клиента, запрашиваемое доменное имя и тип запроса. Формат записи обеспечивает быстрый поиск и фильтрацию событий при анализе журнала.

Адрес клиента извлекается из структуры #emph("sockaddr"), возвращаемой функцией #emph("recvfrom()"). Для адресов #emph("IPv4") используется функция #emph("inet_ntop()"), преобразующая бинарное представление адреса в текстовую форму. Полученная строка включается в запись журнала, что позволяет идентифицировать источник запроса. Такая информация полезна при диагностике некорректных запросов, анализе географического распределения клиентов и выявлении попыток несанкционированного доступа.

Доменное имя и тип запроса извлекаются в процессе разбора #emph("DNS")-пакета. Запись в журнал формируется после успешного извлечения этих данных, но до выполнения поиска в кэше или зоне. Это гарантирует, что даже запросы, приводящие к ошибкам на последующих этапах обработки, будут зафиксированы в журнале.

Типичная запись журнала имеет вид: "Получен запрос от #emph("192.0.2.1"): домен #emph("example.com"), тип #emph("A")". Такая структура обеспечивает удобочитаемость при ручном анализе и позволяет автоматизировать обработку журнала средствами регулярных выражений или специализированных инструментов анализа логов.

=== Логирование hit/miss кэша

Эффективность кэша является важным показателем производительности сервера. Фиксация событий попадания и промахов кэша позволяет оценить долю запросов, обслуживаемых без обращения к файлу зоны, и принять решение о необходимости настройки параметров кэширования.

Событие попадания в кэш (#emph("cache hit")) регистрируется, когда запрошенная пара "доменное имя, тип записи" обнаружена в кэше, и соответствующая запись не истекла. Запись в журнале содержит доменное имя и тип запроса, а также указание на успешное извлечение данных из кэша. Типичный формат: "Кэш: попадание для #emph("example.com") (#emph("A"))".

Событие промаха кэша (#emph("cache miss")) регистрируется, когда запрошенная запись отсутствует в кэше или оказалась устаревшей. Запись в журнале указывает на необходимость обращения к файлу зоны. Формат записи: "Кэш: промах для #emph("example.com") (#emph("A"))".

Анализ соотношения попаданий и промахов кэша позволяет оценить эффективность распределения памяти, выделенной под кэш. Высокая доля промахов может свидетельствовать о недостаточном размере кэша или о коротком времени жизни записей. Напротив, чрезмерно большой кэш с незначительным количеством промахов может указывать на излишнее потребление памяти без соответствующего выигрыша в производительности.

=== Логирование ошибок парсинга

Некорректные или повреждённые пакеты, поступающие на сервер, должны быть зафиксированы в журнале для последующего анализа. Ошибки парсинга могут возникать вследствие нарушения протокола клиентом, повреждения данных при передаче по сети или целенаправленных попыток эксплуатации уязвимостей.

Наиболее частыми причинами ошибок парсинга являются: некорректное значение счётчика секций запроса (#emph("QDCOUNT") не равен единице), установленный флаг #emph("QR") в запросе (пакет является ответом, а не запросом), недостаточная длина пакета (размер датаграммы меньше минимального размера #emph("DNS")-заголовка), некорректное кодирование доменного имени (указатель на несуществующее смещение или циклическая ссылка в механизме сжатия).

При обнаружении ошибки парсинга формируется запись в журнале с указанием типа ошибки, адреса клиента и, по возможности, содержимого некорректного поля. Типичный формат записи: "Ошибка парсинга: некорректный #emph("QDCOUNT") (значение: 2) от #emph("192.0.2.1")". Если разбор пакета не удалось завершить из-за нарушения структуры, в журнал записывается общее описание ошибки: "Ошибка парсинга: пакет слишком короткий от #emph("192.0.2.1")".

Фиксация ошибок парсинга позволяет выявить некорректно настроенные клиенты, обнаружить сетевые проблемы, вызывающие повреждение данных, и зафиксировать попытки атак на сервер. Частое появление ошибок от одного и того же адреса может служить основанием для блокировки источника на уровне межсетевого экрана.

=== Формат логов

Унифицированный формат записей журнала обеспечивает удобство ручного анализа и автоматической обработки. Каждая запись начинается с метки времени, за которой следует уровень сообщения и текст события.

Метка времени формируется с точностью до секунды в формате #emph("ISO 8601"): "ГГГГ-ММ-ДД ЧЧ:ММ:СС". Использование стандартного формата позволяет корректно сортировать записи и интегрировать журнал с системами централизованного сбора логов. Метка времени извлекается с использованием функций стандартной библиотеки #emph("C++"), таких как #emph("std::chrono::system_clock::now()") и #emph("std::put_time()").

Уровень сообщения классифицирует событие по степени важности. Выделяются следующие уровни: #emph("INFO") -- информационные сообщения о нормальной работе сервера (получение запроса, попадание в кэш), #emph("WARN") -- предупреждения о нештатных ситуациях, не приводящих к отказу обслуживания (промах кэша, запрос несуществующего домена), #emph("ERROR") -- ошибки, препятствующие корректной обработке запроса (ошибки парсинга, недоступность файла зоны).

Типичная структура записи журнала: "2026-03-25 14:32:01 [#emph("INFO")] Получен запрос от #emph("192.0.2.1"): домен #emph("example.com"), тип #emph("A")". Уровень сообщения заключается в квадратные скобки для визуального выделения, что упрощает фильтрацию записей при просмотре журнала.

Вывод журнала осуществляется в стандартный поток вывода или в файл, в зависимости от конфигурации сервера. При записи в файл рекомендуется использовать механизмы ротации логов, предотвращающие неограниченный рост размера журнала. Для этого могут применяться внешние инструменты, такие как #emph("logrotate"), или встроенные механизмы библиотеки логирования.
