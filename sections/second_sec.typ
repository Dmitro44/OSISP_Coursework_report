#import "../lib/stp2024.typ"

= Платформа программного обеспечения

Операционная система предоставляет абстракции, через которые прикладная программа взаимодействует с аппаратурой, а глубокое понимание этих абстракций является необходимым условием для написания корректного сетевого кода. В данном разделе рассматриваются теоретические основы и обоснование выбора инструментов, применённых при разработке #emph("DNS")-сервера.

== Язык программирования

Среди языков программирования общего назначения #emph("C++") занимает особое место в области системного и сетевого программирования. Главное достоинство языка -- прямой контроль над памятью: программист самостоятельно определяет время жизни объектов, управляет размещением данных в стеке или куче и может работать с сырыми байтовыми буферами без дополнительных уровней абстракции. Это свойство принципиально важно при разработке сетевых серверов, где необходимо напрямую формировать и разбирать двоичные пакеты строго определённого формата.

Производительность #emph("C++") обусловлена компиляцией в нативный машинный код без промежуточного исполнения. В сочетании с отсутствием сборщика мусора это обеспечивает предсказуемые задержки -- качество, критичное для серверного программного обеспечения, где скачки времени отклика недопустимы. Кроме того, #emph("C++") не вводит дополнительных уровней абстракции между прикладным кодом и ядром операционной системы, поэтому вызовы функций стандартной библиотеки для работы с сокетами транслируются напрямую в соответствующие системные вызовы без промежуточных рантаймов.

Для данного проекта используется стандарт #emph("C++20"), расширивший язык рядом инструментов, существенно повышающих безопасность и выразительность кода. Тип #emph("std::span") представляет невладеющий вид на непрерывную последовательность элементов: он передаёт одновременно указатель и длину, исключая тем самым возможность выхода за границы буфера. Контейнер #emph("std::vector") обеспечивает автоматическое управление временем жизни динамических массивов, а #emph("std::unordered_map") -- хранение записей зоны с амортизированным доступом за константное время. Для организации конкурентного доступа к разделяемым данным применяется #emph("std::shared_mutex"), позволяющий различать операции чтения и записи и не блокировать читателей друг относительно друга. Потоки выполнения создаются средствами #emph("std::thread"), входящего в стандартную библиотеку и не требующего внешних зависимостей.

== Операционная система

Операционная система #emph("Linux") является доминирующей платформой для серверного программного обеспечения. Её ядро реализует полный стек сетевых протоколов и предоставляет прикладным программам устойчивый интерфейс системных вызовов, основанный на стандарте #emph("POSIX"). Благодаря этому интерфейсу операции над файлами, процессами, сигналами и сетевыми соединениями унифицированы таким образом, что код, написанный для одной #emph("POSIX")-совместимой системы, может быть перенесён на другую с минимальными изменениями.

Для работы с сетью #emph("POSIX") определяет интерфейс сокетов, в который входят системные вызовы #emph("socket"), #emph("bind"), #emph("recvfrom") и #emph("sendto"), доступные на любой #emph("Linux")-системе. Ядро #emph("Linux") реализует эти вызовы с высокой эффективностью, используя внутренние механизмы буферизации, оптимизированные очереди пакетов и встроенную поддержку многоядерной обработки.

Для разработки #emph("DNS")-сервера #emph("Linux") удобен по нескольким причинам. Во-первых, порт 53, стандартный для #emph("DNS"), в пространстве адресов ниже 1024 является привилегированным, а #emph("Linux") предоставляет гибкие механизмы управления привилегиями, позволяющие запускать сервер с минимально необходимым набором прав. Во-вторых, богатая экосистема инструментов командной строки -- #emph("dig"), #emph("tcpdump"), #emph("ss") -- даёт возможность немедленно проверять поведение сервера на уровне сетевых пакетов. В-третьих, именно на #emph("Linux") сосредоточена основная часть промышленных реализаций #emph("DNS"), что делает эту платформу естественной средой для разработки и тестирования совместимости.

== Принципы сетевого программирования на базе сокетов

Взаимодействие между узлами сети в современных системах строится на основе многоуровневой модели протоколов, где каждый уровень решает строго определённую задачу и предоставляет вышестоящему уровню абстракцию, скрывающую детали реализации. Для прикладного программиста ключевым является интерфейс на границе между транспортным и прикладным уровнями -- именно здесь располагается программный интерфейс сокетов.

Сокет представляет собой программную абстракцию конечной точки сетевого обмена: через него приложение отправляет данные в сеть и принимает данные из неё, не зная ничего о маршрутизации, фрагментации или физической среде передачи. Интерфейс #emph("BSD Socket API"), предложенный в рамках реализации #emph("BSD Unix") и вошедший в стандарт #emph("POSIX"), является стандартом де-факто для всех современных операционных систем. Единообразие этого интерфейса означает, что код сетевого приложения остаётся переносимым вне зависимости от конкретной платформы.

Различие между #emph("UDP")- и #emph("TCP")-сокетами носит концептуальный характер. #emph("TCP") предоставляет надёжный поток байт с гарантией доставки, упорядоченностью и контролем перегрузки -- это достигается установлением соединения и поддержанием его состояния на обоих концах. #emph("UDP"), напротив, является протоколом без установления соединения: каждая датаграмма независима, отправляется и принимается как единое целое, а гарантии доставки отсутствуют. Для #emph("DNS") такой режим работы является предпочтительным, поскольку запрос и ответ умещаются в один пакет, а повторная попытка при потере пакета реализуется на стороне клиента.

Важным аспектом сетевого программирования является порядок байт. Процессоры с архитектурой #emph("x86") используют порядок «от младшего к старшему» (#emph("little-endian")), тогда как сетевые протоколы требуют порядка «от старшего к младшему» (#emph("big-endian"), или сетевого порядка байт). При формировании числовых полей заголовков #emph("DNS") каждое многобайтовое значение должно быть явно преобразовано с помощью соответствующих стандартных функций, иначе пакет будет некорректно интерпретирован любым другим участником сетевого обмена.

== Работа с бинарными структурами данных и сериализация

Сериализация -- это процесс преобразования объектов программы в последовательность байт для хранения или передачи по сети, а десериализация -- обратный процесс восстановления объектов из такой последовательности. В контексте сетевых протоколов сериализация является одной из центральных задач: формат передаваемых данных строго регламентирован стандартом, и любое отступление от него делает пакет нечитаемым для других реализаций.

Бинарный формат #emph("DNS")-пакета обладает рядом особенностей, существенно усложняющих сериализацию. Пакет состоит из фиксированного заголовка и нескольких секций переменной длины, поэтому прямое отображение буфера на единую структуру данных невозможно. Доменные имена кодируются в виде последовательности меток, где каждый сегмент предваряется однобайтовым счётчиком его длины, а вся последовательность завершается нулевым байтом. Такое представление требует итеративного разбора и не имеет фиксированного размера.

Дополнительную сложность вносит механизм сжатия имён, предусмотренный стандартом #emph("DNS") для уменьшения размера пакета. Если имя или его часть уже встречалось ранее в том же пакете, вместо повторной записи используется двухбайтовый указатель на смещение внутри пакета. Старшие два бита первого байта отличают указатель от обычного счётчика длины. Логика десериализации обязана распознавать такие указатели и корректно восстанавливать полное имя по цепочке ссылок. При этом необходимо учитывать возможность намеренно сформированных циклических указателей, что требует явного ограничения глубины обхода.

Отдельного внимания требует вопрос выравнивания структур в памяти. Компилятор вправе вставлять незначащие байты между полями структуры для выравнивания по границам машинного слова, что ускоряет доступ к памяти. Однако при работе с сетевыми пакетами это недопустимо: стандарт определяет положение каждого поля с точностью до одного бита, и любое «паразитное» выравнивание нарушает совместимость. Управление выравниванием осуществляется директивами компилятора.

Стандарт #emph("C++20") предоставляет удобные инструменты для безопасной работы с двоичными буферами. Тип #emph("std::span<const uint8_t>") позволяет передавать в функции разбора вид на буфер -- одновременно указатель и длину -- без копирования и без риска обращения за его пределы. Контейнер #emph("std::vector<uint8_t>") берёт на себя управление временем жизни буферов ответа, исключая ручное освобождение памяти. Совместное применение этих инструментов позволяет строить надёжный код сериализации, устойчивый к некорректным входным данным.
