#import "../lib/stp2024.typ"

= Архитектура программного обеспечения

В данном разделе рассматриваются ключевые архитектурные решения, принятые при проектировании #emph("DNS")-сервера: выбор транспортного протокола, организация хранения данных зоны и кэширования.

== Структура сетевого взаимодействия на базе протокола UDP

Задача построения высокопроизводительного сетевого сервиса начинается с выбора транспортного протокола. Современный стек сетевых протоколов предлагает два принципиально разных подхода к доставке данных между узлами сети: протокол с установлением соединения (#emph("TCP")) и протокол передачи датаграмм (#emph("UDP")).

Протокол #emph("TCP"), прежде чем начать передачу полезных данных, выполняет процедуру согласования между отправителем и получателем. Эта процедура гарантирует, что обе стороны готовы к обмену, создаёт виртуальный канал связи и обеспечивает надёжную упорядоченную доставку каждого фрагмента данных. Цена такой надёжности -- дополнительные сетевые обмены на установление и завершение соединения, а также накладные расходы на поддержание его состояния в памяти сервера для каждого подключённого клиента.



Протокол #emph("UDP") лишён этих гарантий: каждый пакет является самостоятельным и независимым. Отправитель не знает, доставлен ли пакет, а получатель не обязан принимать пакеты в том же порядке, в котором они были отправлены. Казалось бы, это делает #emph("UDP") непригодным для серьёзных задач. Однако именно отсутствие накладных расходов делает его идеальным для сервисов, работающих по модели «запрос -- ответ». На уровне прикладной логики #emph("DNS") каждое взаимодействие представляет собой пару пакетов: запрос от клиента и ответ от сервера. #emph("UDP") не требует ничего сверх этого, тогда как #emph("TCP") дополнительно генерирует служебный трафик в обоих направлениях -- подтверждения, сегменты установления и завершения соединения, -- необходимый для поддержания состояния протокола. Потеря единственного запроса при использовании #emph("UDP") решается тривиальным повторным обращением на прикладном уровне.

Структура #emph("UDP")-пакета предельно проста и содержит минимальный набор полей, необходимых для доставки данных. Заголовок состоит из четырёх полей фиксированной длины: порт источника (2 байта), порт назначения (2 байта), длина пакета (2 байта) и контрольная сумма (2 байта). Общий размер заголовка составляет 8 байт, что делает #emph("UDP") одним из наиболее эффективных протоколов транспортного уровня. Поле порта источника позволяет получателю отправить ответ, поле порта назначения указывает, какому приложению предназначены данные, поле длины содержит общий размер пакета, включая данные, а контрольная сумма обеспечивает базовую проверку целостности. За заголовком следует полезная нагрузка произвольной длины, содержимое которой интерпретируется на прикладном уровне.

#figure(
  rect(stroke: 1.5pt + black, inset: 0pt)[
    #image("../img/udp-header.jpg", width: 80%)
  ],
  caption: [Структура заголовка #emph("UDP")-пакета]
) <UdpHeaderImg>

Система доменных имён по своей природе строится именно на таком взаимодействии. Согласно стандарту @rfc1035, основным транспортом для #emph("DNS") является протокол #emph("UDP"), функционирующий на порту 53. #emph("DNS")-пакет имеет собственную структуру, отличную от #emph("UDP")-заголовка, и располагается в полезной нагрузке датаграммы.

Взаимодействие между клиентом и сервером в системе доменных имён строится по классической модели «запрос -- ответ» без установления постоянного соединения. Клиент формирует запрос, направляет его на стандартный порт 53 сервера и ожидает ответного пакета. Сервер, в свою очередь, принимает входящие датаграммы, обрабатывает каждую независимо и отправляет ответ непосредственно на адрес отправителя. Такая архитектура клиент-сервер идеально соответствует природе #emph("UDP"): каждый пакет самодостаточен и несёт в себе все необходимые адресные данные для маршрутизации ответа. Порт 53 закреплён за службой #emph("DNS") международным стандартом и является общепризнанным идентификатором данного протокола на транспортном уровне.

== Иерархия DNS-серверов и процесс разрешения запросов

Система доменных имён представляет собой распределённую иерархическую базу данных, организованную в виде дерева. Каждый узел этого дерева соответствует домену и обслуживается одним или несколькими серверами, которые хранят авторитетную информацию о нём. Понимание структуры этой иерархии необходимо для осознания того, как происходит процесс разрешения имени в #emph("IP")-адрес.

Корнем всей системы доменных имён является точка (.), обозначаемая как корневой домен или #emph("root")-домен. Серверы, обслуживающие корневой домен, называются #emph("root")-серверами. Их количество, состав и географическое распределение тщательно контролируются для обеспечения устойчивости всей системы. На момент написания работы функционирует 13 логических #emph("root")-серверов, обозначаемых латинскими буквами от A до M, хотя физически каждый из них представляет собой распределённый кластер с множеством географически распределённых экземпляров. #emph("Root")-серверы не хранят информации о конкретных веб-сайтах. Их единственная функция состоит в направлении запросов к серверам следующего уровня -- серверам доменов верхнего уровня.

Домены верхнего уровня, обозначаемые аббревиатурой #emph("TLD") (#emph("Top-Level Domain")), образуют второй уровень иерархии. Они делятся на две основные категории. Первую составляют общие домены верхнего уровня (#emph("gTLD") -- #emph("generic Top-Level Domain")), к которым относятся такие домены, как #emph("COM"), #emph("ORG"), #emph("NET"), #emph("EDU"), #emph("GOV") и многие другие. Эти домены не привязаны к какой-либо конкретной стране и используются преимущественно коммерческими, общественными и государственными организациями. Вторую категорию образуют национальные домены верхнего уровня (#emph("ccTLD") -- #emph("country-code Top-Level Domain")), идентифицирующие конкретную страну или территорию: #emph("RU"), #emph("UA"), #emph("US"), #emph("DE"), #emph("JP") и т.д. Каждая из этих категорий управляется соответствующим администратором: для #emph("gTLD") это корпорация #emph("ICANN"), для #emph("ccTLD") -- национальные реестры конкретных стран. Серверы #emph("TLD") хранят информацию о доменах второго уровня, которые регистрируются конечными пользователями или организациями.

Авторитативные серверы (#emph("Authoritative DNS Servers")) образуют последний уровень иерархии, с которым непосредственно взаимодействует резолвер при разрешении имени. Авторитативный сервер -- это сервер, который хранит оригинальные записи о домене и имеет право отвечать на запросы об этом домене без обращения к другим серверам. @authoritatServ Когда организация регистрирует доменное имя, она указывает список авторитативных серверов для этого домена, которые вносятся в родительскую зону. Например, для домена #emph("example.com") авторитативные серверы указываются в зоне #emph("COM"). Типичная конфигурация включает два или более сервера для обеспечения отказоустойчивости.

Процесс разрешения имени может протекать двумя принципиально разными способами. Первый способ -- рекурсивное разрешение. В этом случае клиент (или рекурсивный резолвер, которым обычно выступает #emph("DNS")-сервер провайдера) берёт на себя всю работу по обходу иерархии: он сам обращается к #emph("root")-серверу, затем к #emph("TLD")-серверу, затем к авторитативному серверу и возвращает готовый результат клиенту. Второй способ -- итеративное разрешение, при котором сервер отвечает клиенту тем, что знает сам, и указывает, к какому серверу следует обратиться дальше. Именно итеративный подход заложен в основу работы рекурсивных резолверов. Они выполняют серию итеративных запросов к различным серверам иерархии, собирая информацию по частям, пока не получат окончательный ответ. Несмотря на многоэтапность, этот процесс занимает миллисекунды благодаря кэшированию на каждом уровне иерархии: однажды полученный ответ сохраняется на время, определяемое полем #emph("TTL") соответствующей ресурсной записи.

Необходимо отметить, что система доменных имён спроектирована с расчётом на отказоустойчивость и масштабируемость. Каждый уровень иерархии может содержать множество серверов, и выход из строя отдельного сервера не приводит к отказу всей системы. Делегирование полномочий от родительской зоны к дочерней позволяет равномерно распределить нагрузку между серверами и масштабировать систему по мере роста пространства имён.

== Формат файла зоны

Авторитативный #emph("DNS")-сервер отличается от рекурсивного резолвера тем, что не ищет ответ путём обхода иерархии, а хранит его локально. Источником этих данных служит файл зоны -- текстовый файл, содержащий набор ресурсных записей для обслуживаемого домена. Понятие файла зоны закреплено стандартом @rfc1035 и реализовано во всех распространённых #emph("DNS")-серверах.

Файл зоны открывается директивами #emph("$ORIGIN") и #emph("$TTL"), задающими базовый домен и время жизни записей по умолчанию. Следом располагается обязательная запись #emph("SOA") (Start of Authority), описывающая параметры зоны: имя первичного сервера, серийный номер и интервалы обновления. Далее следуют ресурсные записи, каждая из которых содержит имя узла, класс #emph("IN"), тип и значение. Пример файла зоны приведён на рисунке @ZoneFileImg.

#figure(
  rect(stroke: 1.5pt + black, inset: 0pt)[
    #image("../img/zone-file.png", width: 80%)
  ],
  caption: [Пример файла зоны]
) <ZoneFileImg>

При запуске сервер разбирает файл построчно, проверяя корректность каждой записи: соответствие формата адресов стандарту, допустимость символов в доменных именах, принадлежность #emph("TTL") допустимому диапазону. Некорректные строки пропускаются с фиксацией ошибки в журнале, что позволяет серверу продолжить загрузку даже при наличии ошибок в отдельных записях.

== Организация кэша в памяти

Кэш в памяти -- это промежуточное хранилище данных, обеспечивающее быстрый доступ к часто запрашиваемым результатам без повторного выполнения дорогостоящих операций их получения. В отличие от дисковых хранилищ, оперативная память обеспечивает доступ к данным за наносекунды, что делает её естественным выбором для хранения рабочего набора данных в высоконагруженных системах. Расплатой за скорость является энергозависимость, ведь содержимое кэша не переживает перезапуск процесса и должно быть восстановлено из первоисточника.

В контексте #emph("DNS")-сервера кэш хранит ресурсные записи, полученные ранее в ответ на запросы клиентов. Это позволяет отвечать на повторяющиеся запросы без обращения к файлу зоны или вышестоящим серверам иерархии. Поскольку множество клиентов периодически запрашивают одни и те же имена, даже небольшой кэш способен значительно снизить нагрузку на сервер.

Ключевой особенностью #emph("DNS")-кэша является ограниченный срок хранения записей, определяемый значением #emph("TTL") @ttl_strategies. Это отражает природу самой системы доменных имён: записи в зоне могут меняться, и хранение устаревших данных приведёт к выдаче некорректных ответов. Поэтому каждая запись в кэше сопровождается меткой времени добавления, а при обращении к ней проверяется, не истёк ли срок её жизни.

Многопоточная архитектура сервера накладывает дополнительное требование в виде потокобезопасного доступа к кэшу. При этом параллельное чтение из нескольких потоков безопасно, тогда как запись требует монопольного доступа. Разграничение этих двух режимов позволяет избежать состояния гонки, не создавая излишних ограничений на параллельность операций чтения.
